# Algora Bounty Hunter - 赏金猎手框架

> 自动化的 GitHub Issue 修复工具，帮助你参与 Algora.io 赏金任务

---

## 项目概述

这个框架可以自动：
1. 从 GitHub 搜索带有 bounty 标签的 Issue
2. 对 Issue 进行可行性分析（规则过滤 + AI 评分）
3. 克隆仓库并使用 Claude Code 自动修复 Issue
4. 使用你的 GitHub 账号提交 PR
5. 推送到远程仓库

**你需要做的**：在 Algora.io 上认领赏金并关联 PR

---

## 快速开始

### 1. 安装依赖

```bash
cd /home/eric/桌面/workspace/claude-agent-framework
npm install
```

### 2. 运行框架

```bash
# 运行 1 个周期
npm start 1

# 运行 10 个周期
npm run start:10
```

### 3. 查看结果

```bash
# 查看任务状态
cat tasks.json

# 查看日志
tail -f logs/bounty-hunter-*.log
```

---

## 工作流程

```
┌─────────────────────────────────────────────────────────────────┐
│                        框架自动完成                              │
├─────────────────────────────────────────────────────────────────┤
│  1. 搜索 GitHub bounty 相关 Issue                                │
│  2. 规则过滤（描述清晰度、代码示例等）                         │
│  3. AI 评分（可行性分析）                                       │
│  4. 克隆仓库到 /tmp/bounty-hunter/                             │
│  5. Claude Code 自动修复 Issue                                  │
│  6. 配置你的 GitHub 账号信息                                     │
│  7. Commit & Push 到 GitHub                                     │
│  8. 清理临时目录                                                │
└─────────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│                        你需要手动完成                            │
├─────────────────────────────────────────────────────────────────┤
│  1. 打开 algora.io                                             │
│  2. 找到对应的赏金任务                                          │
│  3. 点击 "Claim" 认领赏金                                      │
│  4. 关联框架创建的 PR                                           │
│  5. 等待审核和打款                                              │
└─────────────────────────────────────────────────────────────────┘
```

---

## 配置说明

编辑 `config.json` 可以调整参数：

### 搜索关键词

```json
"github": {
  "searchKeywords": ["bounty", "reward", "paid", "hackathon", "grants"],
  "perPage": 30
}
```

### 分析阈值

```json
"analysis": {
  "minRuleScore": 30,      // 规则过滤最低分数
  "minAiScore": 50,        // AI 分析最低分数
  "minTotalScore": 60      // 综合最低分数
}
```

### 执行参数

```json
"execution": {
  "timeLimits": {
    "simple": 15,    // 简单 Issue：15 分钟
    "medium": 45,   // 中等 Issue：45 分钟
    "complex": 90   // 复杂 Issue：90 分钟
  },
  "maxCommitsWithoutProgress": 10,  // 无进展最大 commit 数
  "tempDir": "/tmp/bounty-hunter"   // 临时目录
}
```

---

## GitHub 账号配置

框架已配置使用以下账号提交 PR：

| 字段 | 值 |
|------|-----|
| 用户名 | EricYinhy |
| 邮箱 | erichey@163.com |

这些信息会在每次 commit 时自动配置到临时仓库中。

---

## 项目结构

```
claude-agent-framework/
├── CLAUDE.md              # AI 工作流程说明
├── config.json            # 配置文件
├── package.json           # npm 配置
├── tsconfig.json          # TypeScript 配置
├── progress.txt           # 进度日志
├── tasks.json            # 任务状态（自动生成）
├── src/
│   ├── coordinator.ts    # 主协调器
│   ├── agents/
│   │   ├── issue-fetcher.ts        # Issue 获取
│   │   ├── rule-filter.ts          # 规则过滤
│   │   ├── ai-analyst.ts           # AI 分析
│   │   ├── code-agent-for-issue.ts # 修复 Issue
│   │   └── code-agent-for-commit.ts # Git 提交
│   ├── config/config.ts
│   ├── types/index.ts
│   └── utils/
│       ├── github.ts
│       └── logger.ts
└── logs/                 # 日志文件
```

---

## 重要说明

### 关于 Algora.io

- Algora.io **没有公开的 API**
- 赏金认领需要**手动**在网站上完成
- PR 会通过 GitHub 账号自动关联到你的 Algora 账户

### 当前限制

1. **AI Analyst 是 Placeholder**
   - 当前返回固定分数（75分）
   - 后续可以接入真实的 LLM API

2. **网络要求**
   - 需要能够访问 GitHub
   - 如果遇到网络问题，检查代理或 VPN

---

## 常见问题

### Q: 运行时报错 "Nested sessions"

A: 这是 Claude Code 的安全限制。代码已经处理了这个问题，通过删除 CLAUDECODE 环境变量来绕过。

### Q: Git 提交失败 "Author identity unknown"

A: 框架会自动配置 Git 用户信息。如果还有问题，确保已安装 Git 并配置正确。

### Q: 如何修改搜索关键词？

A: 编辑 `config.json` 中的 `searchKeywords` 数组。

### Q: 如何修改时间限制？

A: 编辑 `config.json` 中的 `execution.timeLimits`。

---

## 日志查看

```bash
# 实时查看日志
tail -f logs/bounty-hunter-2026-02-23.log

# 查看所有日志
ls -la logs/

# 查看任务状态
cat tasks.json
```

---

## 下一步

1. 运行 `npm start 1` 测试
2. 在 Algora.io 上找到对应赏金并认领
3. 关联 PR，等待审核和打款

---

## 技术栈

- **运行环境**: Node.js + TypeScript
- **执行工具**: Claude Code CLI
- **API**: GitHub REST API
- **配置文件**: JSON

---

**祝你玩得开心，获得丰厚赏金！** 🎉
